{
  "name": "vireon-ai",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "postinstall": "pnpm --filter @vireon/db generate"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "@sentry/cli",
      "esbuild",
      "prisma",
      "sharp"
    ]
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prisma": "^7.8.0",
    "turbo": "^2.0.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0"
  }
}
